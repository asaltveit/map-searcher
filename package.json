{
  "name": "map-searcher",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "lint": "pnpm -r run lint:check",
    "test": "pnpm -r run test",
    "test:server": "pnpm --filter server-nest test",
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck"
  }
}
