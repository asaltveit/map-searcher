# Backend (server-nest) reads these from server-nest/.env when you run from server-nest/.
# Copy this file to server-nest/.env and set values there (or use a single .env at root if you run from root).

# Letta API key (https://app.letta.com/api-keys)
LETTA_API_KEY=sk-let-NTMzM2NhYjUtMTBiNi00YTIxLTlhZWYtNzlhYTU4Mzc2N2I0OmVjMDQ1OTgzLTk5MTQtNGRjNy04YTI3LTk2ODE5MDNhYmViYg==

# Redis URL (e.g. redis://localhost:6379)
REDIS_URL=redis://localhost:6379

# Database (required by server-nest/Prisma). Example: postgresql://user:pass@host:5432/dbname?sslmode=require
DATABASE_URL=postgresql://localhost:5432/map_searcher

# W&B Weave API key for tracing (https://wandb.ai/settings)
WANDB_API_KEY=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
# Optional: your W&B username (or team). Set this if you see "Default entity name not found".
# Then traces go to entity/map-searcher (e.g. myuser/map-searcher) on wandb.ai.
WANDB_ENTITY=annasaltveit-personal

# Optional: enable hybrid multi-user (per-user agents + my research)
# If set, all /api/research and /api/agents require X-User-Id header (or cookie)
ENABLE_HYBRID_MULTI_USER=false

# Optional: improvement storage API (submit JWT + improvement string; built separately)
# POST /api/improvements forwards Authorization + body to this URL
# IMPROVEMENT_STORAGE_URL=

# Optional: server-nest port (default 3000). Use PORT=3001 when client runs on 3000.
# PORT=3001

# Optional: client API base URL in production (Next.js). Set NEXT_PUBLIC_API_URL in client env.
# NEXT_PUBLIC_API_URL=https://your-api.fly.dev
